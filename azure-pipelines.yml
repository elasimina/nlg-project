# Node.js with Angular
- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'Visual Studio Professional Subscription(382ab373-5f37-4f6a-b627-aae6f447d436)'
    appType: 'webApp'
    WebAppName: 'deploymed'
    packageForLinux: '/dist'
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install -g @angular/cli
    npm install
    ng build --prod
  displayName: 'npm install and build'

- task: AzureRmWebAppDeployment@4
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'Бесплатная пробная версия(1)(2ef8fb7f-acea-43e5-be3a-444214fd0329)'
    appType: 'webApp'
    WebAppName: 'meditations-ai'
    packageForLinux: 'dist'
